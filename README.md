# Rearm

A small, clean and controller PHP utility that gives failed operations another shot.

## Usage (without Composer / Packagist)

1. Clone the repository:

```bash
git clone https://github.com/maxster646/rearm.git